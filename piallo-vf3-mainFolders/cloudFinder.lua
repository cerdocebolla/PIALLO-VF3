when askedForFunction = setTrue = true 
then let function = setAslocal.chain 
    when goDown then createChain = forceToTrue = true
        when chain = setAs sealed 
        and created 
        if 

    function = runningTheSoftware = true 
        then createChain in 
            as down 
            localServers.cloudServers.AlwaysAccess.localClient.client.Servers.server.mainDevice.mainUsers.user = true 
            then let 
                function endInstant.end)
            end)
        end)
    end)
}
else local client.function = false 
    then function = let print 
        print ("Couldn't find the cloud servers around, please stand by for an official fixage or try again.")
        then as fucntion 
            let function endInstant.end
        end)
    end)
}

when function = letPrintOut = makeTrueWhenSure = accessMade
    connects 
    as accessMade = makeTrueWhenSure = true 
    
    as function = whenSure 
        function 
            let print ("The PIALLO Software has successfully accessed into the cloud servers. Running PIALLO...")
            then function 
                function endInstant.end
            end)
        end)
    end)
}