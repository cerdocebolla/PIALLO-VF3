local function.falseMovement.mainDevice.device.detect = detected = true 
    then try.setTrueEverytime

    else askForParent = chainedWiFi = true (setTrueEverytime)
        if falseMovement = detected.setAnswer = true 
        then runningSoftware.force = false (forceToFalse)
            then print ("An error has occured while trying to run the PIALLO Software. Please try again.")
    then endInstant.end 
            end)
        end)
    end)
    
    then askForChild.connectWiFi = setTrueEverytime 
        if mainUserDpi.mainDevice = setAnswer.true = true 
        then askForParent.connection.force = true (forceToTrue)
            then print ("You've bypassed the error test, now you can use the PIALLO Software safely.")
    then endInstant.end
            end)
        end)
    end)
end)