local function.askForProtection.LocalClient - setApp 
as CloudFlare = allowAccessToTheScript.force.setTrue = true (alwaysSetTrue)
                when askedForParent = protectedWiFi.true.forceToTrue = setTrue.force = true 
                then let function = CloudFlare.allowAccessToTheScript = alwaysSetTrue.force true 
                for CloudFlare
            when accessGiven.connectClient.localClient.LocalDevice = seperateParent.chainedWiFi = true (alwaysSetTrue)
            set getParent.force = true 
            set parent 
            as CloudFlare = forceToTrue = true (alwaysSetTrue)
    when accessGiven.controlClient.LocalWiFi = setMainDevice as 
    goInBracket = (seperateParent.SetParent = Modem = forceToTrue) = true (alwaysSetTrue)
    when Modem.connectedAsDevice then 
    SetMainDevice.chainedDevice.ChainedClient.LocalServers = accessGiven for 
    CloudFlare = true 
    then let endInstant
}
    else runSoftware
    for CloudFlare 
        try.RunningAlways.SoftwareChain.blockChain = true 
        when 
        as setTrue then let 
    endInstant
}