local function = consolePort goDown 
    mustNeed = mustHave 
    set function then goDown 
        function = consolePort 
            if localClient.localServers.localSoftware = allow 
            and setTrue = true 
            then let print ("Running the PIALLO Software for your console, please be patient.")
                when consolePort = activated 
                as WiFi = true 
                then set chain = function 
                    wifiConnector.consolePort.chainedWiFi = port goDown (readDown.checkDown)
                    consolePort = allow 
                    and access function = port 
                        for gaming functions goDown 
                        function = gamingDevices 
                            when checkUp then readDown = alwaysAccess = setTrueEverytime = setTrue.force = true 
                                xbox
                                playstation
                                
                                then function = readUp = true 
                                    then setGamingFunctions = true 
                                        after software = true 
                                        then askForChild = setTrue = true 
            then funtion = endInstant.end 
                                        end)
                                    end)
                                end)
                            end)
                        end)
                    end)
                end)
            end)
        end)
    end)
}                 